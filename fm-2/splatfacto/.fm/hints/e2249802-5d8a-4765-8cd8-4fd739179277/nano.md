Educational infographic, clean flat design, 16:9 landscape, soft off-white background, modern sans-serif typography, pastel blue and coral accent colors.

Title at top center: "Gaussian Densification: Clone or Split? (복제 vs 분할)"

Below the title, a single trigger badge in the center: a small lightning icon inside a rounded pill labeled "High positional gradient (위치 그래디언트 큼)". From this badge, one arrow flows down into a diamond decision node labeled "Size < 0.01 ?" with a small tag underneath reading "densify_size_thresh".

From the decision diamond, two arrows branch left and right into two comparison panels of equal size.

LEFT PANEL (pastel blue card, rounded corners), header "YES → CLONE (복제)":
- A small blue gaussian blob drawn as a tiny soft ellipse, labeled "Small gaussian".
- A rightward arrow labeled "Duplicate".
- Result: two identical tiny blue ellipses side by side, labeled "Identical copy added".
- Footer caption in small text: "Fixes under-reconstruction".

RIGHT PANEL (coral card, rounded corners), header "NO → SPLIT (분할)":
- A large coral gaussian blob drawn as one big soft ellipse, labeled "Large gaussian".
- A rightward arrow labeled "Split into 2 samples" with a small tag "n_split_samples = 2".
- Result: two smaller coral ellipses sampled inside the outline of the original big ellipse (shown as a dashed silhouette), labeled "Smaller, scale reduced".
- Footer caption in small text: "Fixes over-reconstruction".

Visual style: flat vector shapes with soft gradients on the gaussian blobs, thin dark-gray arrows, generous white space, crisp short labels only, no long sentences, no extra decorative text.
