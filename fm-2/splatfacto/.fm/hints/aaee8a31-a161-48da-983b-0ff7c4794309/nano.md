Educational infographic, clean flat design, 16:9 landscape, soft off-white background, modern sans-serif typography, two-column comparison layout with a header banner.

Title banner at top center: "Splatfacto Densification Strategies" with a small subtitle "strategy = default | mcmc".

Left panel (blue accent color), header label: "default (3DGS heuristic)". Korean sub-label under header: "휴리스틱". Visual: a flow of three steps drawn with small fuzzy ellipse blobs representing 3D Gaussians:
1. A small blob with an upward arrow and label "High gradient" leading to two copies, labeled "Duplicate (small)".
2. A large blob splitting into two smaller blobs, labeled "Split (large)".
3. A faded transparent blob crossed out with an X, labeled "Cull low opacity".
Small footer badge in the left panel: "gsplat DefaultStrategy".

Right panel (orange accent color), header label: "mcmc (3DGS-MCMC)". Korean sub-label under header: "샘플링". Visual elements:
1. A blob with a wiggly dotted trajectory arrow around it, labeled "Noise injection".
2. A faded dead blob with a curved arrow moving it next to a bright blob, labeled "Relocate dead ones".
3. A container box holding many blobs with a max-fill line, labeled "Cap: max_gs_num".
Small footer badge in the right panel: "gsplat MCMCStrategy".

Bottom strip across full width, light red background: a warning triangle icon and short label "Other value -> ValueError".

Style: minimal flat vector icons, generous white space, thin divider between the two panels, consistent rounded rectangles for panels, labels short and crisp, no long sentences, high readability.
