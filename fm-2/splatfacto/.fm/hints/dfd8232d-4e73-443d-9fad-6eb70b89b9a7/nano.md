Educational infographic, clean flat design, 16:9 landscape, soft off-white background, modern sans-serif typography, subtle drop shadows, pastel accent colors (blue, green, orange).

Title at top center in bold dark text: "Splatfacto Method Variants" with small Korean subtitle "세 가지 등록 메서드".

Layout: one shared foundation bar plus a 3-column comparison panel. Eye flow: title, then foundation bar, then three columns left to right.

Foundation bar directly under the title, spanning full width: a wide rounded rectangle in neutral gray-blue containing a single Gaussian splat icon (fuzzy colorful ellipse cloud) and the label "Shared: SplatfactoModel". Three arrows go down from this bar into the three columns, showing all variants inherit the same model.

Three equal columns below, each a rounded card:

Column 1 (blue accent), header "splatfacto": badge label "Baseline". Icon: a balanced scale. Small config chips listed vertically: "cull_alpha 0.1", "densify_grad 0.0008", "strategy: default". Footer tag: "Balanced quality".

Column 2 (green accent), header "splatfacto-big": badge label "More Gaussians". Icon: a dense cluster of many small ellipses, visibly more dots than column 1. Config chips highlighted with green outline: "cull_alpha 0.005", "densify_grad 0.0005". Footer tag: "Higher quality (고품질)". Small downward arrow icons next to the two values to show thresholds lowered.

Column 3 (orange accent), header "splatfacto-mcmc": badge label "MCMC Strategy". Icon: a dice with sampling arrows (stochastic sampling motif). Config chips highlighted with orange outline: "strategy: mcmc", "cull_alpha 0.005", "stop_split_at 25000". Footer tag: "Sampling-based relocation".

Bottom strip: a thin caption band reading "Same model, different config" with Korean "같은 모델, 다른 설정".

Keep all labels short, crisp, legible; monospace font for config chips; generous white space; no photorealism, vector illustration style.
