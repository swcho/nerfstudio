Educational infographic, clean flat design, 16:9 landscape, white background, soft pastel palette (teal, orange, gray), thin rounded outlines, sans-serif labels.

Title at top center: "Gaussian Densification: Clone vs Split" with small Korean subtitle "복제 vs 분할".

Layout: a decision flow reading left to right in three stages.

Stage 1 (left column): a single fuzzy ellipse labeled "Gaussian i" with a meter icon below it labeled "avg screen gradient" and the formula label "grad2d / count". A diamond decision node reads "> 0.0008 ?" (Korean: "그래디언트 임계값").

Stage 2 (middle column): from the "YES" branch, a second diamond labeled "max scale <= 0.01 ?" (Korean: "크기 임계값"), with a small ruler icon showing the longest axis of the ellipse. From the "NO" branch of stage 1, a gray arrow goes down to a bottom row.

Stage 3 (right column), two contrasting panels stacked vertically plus a bottom gray row:
- Top panel, teal, labeled "CLONE (복제)": on the left a small ellipse with a red arrow marked "high grad"; an arrow "copy" leads to two identical small ellipses side by side, same size, one drawn with a "+1" badge. Caption label: "same size, original kept".
- Middle panel, orange, labeled "SPLIT (분할)": on the left a large ellipse with a red arrow marked "high grad"; an arrow "sample 2, scale / 1.6" leads to two smaller ellipses placed inside the faded dashed outline of the original, and the original outline has an "X" mark labeled "original removed".
- Bottom gray row labeled "NO CHANGE (유지)": a plain ellipse with a tiny green check and label "low gradient".

Small footer note in a rounded box: "scale threshold x scene_scale" and "clone first, then split".

Visual emphasis on contrast: small-vs-large input ellipse, same-size pair versus smaller pair, and the removed original in the split panel. No long sentences, all labels 3-5 words.
